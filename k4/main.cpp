#include <iostream>

using namespace std;

int main()
{
 int length1=7.68;

  cout << "the int length is "<<length1<<"\n";

  float length2 =7.68;

  cout <<"the float length is "<<length2<<endl;

        /*we can see the two variables have the same value
   but the change of the data type makes change in the output */
}
